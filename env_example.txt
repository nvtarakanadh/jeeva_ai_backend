# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration - REQUIRED (PostgreSQL only)
# For local development with PostgreSQL:
DATABASE_URL=postgresql://username:password@localhost:5432/jeeva_db

# For production (Render/Railway):
# DATABASE_URL is automatically provided by the platform
# Or set manually: postgresql://user:password@host:port/database

# Supabase Migration (optional - only if migrating from Supabase)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key

# AI API Keys
FIRECRAWL_API_KEY=fc-08be9ac92ea34916b337539a1225fb05
GEMINI_API_KEY=AIzaSyD8MiivYlzS9_fZ9SE_et_iimBih5u33sg
DR7_API_KEY=sk-dr7-f4bef860daaa3ba63042361b768e75dc781a43ada9d6d4a9f316f1985a9a2ccd

# CORS Settings (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8080,http://127.0.0.1:8080

# Email Configuration (for password reset and notifications)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@jeeva.ai

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:3000

# Production Settings (uncomment for production)
# DEBUG=False
# ALLOWED_HOSTS=your-domain.com,*.railway.app,*.render.com
# FRONTEND_URL=https://your-frontend-domain.com
